# Review after 50 cases (SCJ-488 … SCJ-537)

**Do this when the loop has finalized 50 cases under the post-388–437 pipeline (6.8 stencil + BILL/RELEGATE cues live).** Do not skip. The loop prints `REVIEW DUE` and `python tools/log_scj_review.py --summary` dumps the tracked numbers.

Live metrics (gitignored): `supply-code/tmp/pipeline_review/metrics.jsonl`  
Previous batch: `sessions/2026-09-01-review-388-437.md` (SCJ-388–437).  
SCJ-438–487 ran *before* that review was filled; metrics for 438–487 were dropped.

Aim: **minimise tokens and time without compromising quality.**

## What changed (this batch)

1. **6.8-assessment-hearing stencil is LIVE.** Recovery citation + no hearing + deposit + Assessing Officer (or “respondent No. N under Clause 6.8”) + disposed, pages ≤ 2, words ≤ 500. Fill is `fill_68` (`alternate_remedy` / `ordinary`, `UP-2005::6.8`). Quash / s.135 / listing / GRANT stay off. Dry-run before wire: TP=11 FN=0 FP=0 on extracts (SCJ-424…439 cluster). Pending Lucknow queue had 0 would-be 6.8.
2. **BILL + RELEGATE cues live on 6.5.** BILL now also matches `wrong bill`, `incorrect … bill`, `electricity amount due`, `unpaid electrical/electricity dues`, `recovery citation/certificate/notice`. RELEGATE now also matches “should file a challenge”, “can get the bill corrected”, “more appropriately examined”, “if the petitioner approaches”, “applies for correction”. Dry-run gained SCJ-399, 408, 468, 487; veto set (283/284/288/379/385/411) hit 0. SCJ-418 (6.5, no bill in the order) stays off.
3. Unchanged: GRANT veto (SCJ-411); stencil write fail **demotes**; `citation_count = max(ik, text)`; words ≤ 800 still short even with citations. Listing-only still off. Do not widen into 7.10 or 6.14 from this batch unless the 6.8 FP check is clean.

## Tracked parameters

`python tools/log_scj_review.py --summary` after 50. Fields on each loop row:

| Field | Why |
|---|---|
| `authoring` / `gate` | stencil vs short-pages vs short-words vs short-uncited vs full |
| `family` | `6.5-billing-relegation` vs `6.8-assessment-hearing` vs `contempt-6.5-dismissed`. Summary prints `stencil_families`. |
| `elapsed` | stencil ~7s; 6.8 should match 6.5, not short (~116s) |
| `pages`, `words` | 6.8 must stay ≤ 2 pages / ≤ 500 words |
| `citations`, `ik_citations`, `text_citations` | PDFs should show `text>0` when the order cites a case |
| `demoted` / `demoted_from` | fill_68 caption miss should demote, not retry; 358 must not recur |
| `coerce` | should stay near empty |
| `safety_finalize` | still a time leak if Grok skips finalize |
| `outcome`, `significance` | 6.5 and 6.8 stencil must stay `alternate_remedy` / `ordinary`. Summary prints those for stencil rows. |
| `ok` | failures vs last batch |

## Review checklist

- [ ] `python tools/log_scj_review.py --summary` — elapsed by authoring; `stencil_families`; `demoted`; `citations>0` / `text>0`; short-uncited count.
- [ ] **6.8 false positives:** every `family=6.8-assessment-hearing` — extract vs JSON. Fail if the Court **quashed** the assessment/recovery (SCJ-367-class), decided s.135 (375/376-class), listed, withdrew, or granted reconnect. JSON must be `alternate_remedy` / `ordinary` / `UP-2005::6.8`. Deposit and recovery amounts in the digest should match the order (fill_68 slots).
- [ ] **6.8 false negatives:** 2-page “recovery citation / no hearing / deposit / Clause 6.8” that stayed on **short**. If several, the regex is short (officer phrasing, “deposit” vs “depositing Rs”). Do not drop the quash/s.135 veto to chase them.
- [ ] **6.5 false positives:** every `family=6.5-billing-relegation` — extract vs JSON. Fail if the court refused 6.5, listed, withdrew, **granted** (411-class), or decided the bill. New cues (wrong bill / amount due / recovery citation / “should file a challenge”) should be TPs, not FPs.
- [ ] **GRANT veto over-veto:** true 6.5 clones that now miss because the order says “forthwith” in a reconnection/stay sentence. If several, narrow the regex; do not drop `we intervene`.
- [ ] **BILL+RELEGATE over-catch:** 6.5 with **no** bill/demand (SCJ-418-class) must still be short, not stencil. Restore/reconnect in the *prayer* while the Court relegates is OK as 6.5; a merits reconnect (SCJ-385) must not stencil.
- [ ] **Demotes:** for every `demoted=1`, skim extract vs JSON. Fail if the loop retried stencil on the same id. Watch fill_68 empty caption (Allahabad `Petitioner :-` should already work).
- [ ] **Short-uncited that should have been full:** 3-page orders with **text citations** must **not** be `gate=short-uncited`. If `citation_count=0` on a 3-pager that names a reported case, tighten the counter, do not widen short-uncited.
- [ ] **6.5 clones still on short:** 1–2 page “apply under 6.5” with a bill/demand that did *not* stencil *after* the new cues. If several, the regex is still short; do not widen into refusals or 7.10.
- [ ] **Coerce rate:** should stay low.
- [ ] **SCJ-328-class:** 4-page uncited doctrinal quash stays `gate=full` if words > 800. A 3–4 page quash with words ≤ 800 is still `short-words` (watch SCJ-430-class; don’t raise the floor unless quality dips).

Do not turn on listing-only, 7.10, or 6.14 from this review unless the 6.8 **and** 6.5 FP checks are clean *and* a separate dry-run on the pending queue agrees.
