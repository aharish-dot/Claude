# Review after 50 cases (SCJ-488 … SCJ-537)

**Do this when the loop has finalized 50 cases under the post-388–437 pipeline.** Do not skip. The loop prints `REVIEW DUE` and `python tools/log_scj_review.py --summary` dumps the tracked numbers.

Live metrics (gitignored): `supply-code/tmp/pipeline_review/metrics.jsonl`  
Previous batch: `sessions/2026-09-01-review-388-437.md` (SCJ-388–437).  
SCJ-438–487 ran *before* that review was filled; metrics for 438–487 were dropped.

Aim: **minimise tokens and time without compromising quality.**

## What changed (this batch)

1. **GRANT veto.** If the Court grants relief (`we intervene` / `forthwith comply` / `petition is allowed` / `mandamus is issued`), 6.5 in counsel’s mouth is **not** a stencil. SCJ-411 recorded the opposite holding (Lok Adalat compliance, not 6.5 relegation). Dry-run after the veto: FP=0 FN=0.
2. **Logger window** is SCJ-488–537. Do not run the next 50 until this note is the resume point.
3. Unchanged: stencil write fail still **demotes**; BILL still includes current/impugned bill; `citation_count = max(ik, text)`; words ≤ 800 still short even with citations.

Listing-only stays off. Do not widen BILL into “electricity amount due” / restore / refusals / 7.10.

## Tracked parameters

| Field | Why |
|---|---|
| `authoring` / `gate` | stencil vs short-pages vs short-words vs short-uncited vs full |
| `elapsed` | stencil ~7s; GRANT veto must not send true clones to grok |
| `pages`, `words` | confirm gates |
| `citations`, `ik_citations`, `text_citations` | PDFs should show `text>0` when the order cites a case |
| `demoted` / `demoted_from` | still unexercised after 358; each demote still needs a good JSON |
| `coerce` | should stay near empty |
| `safety_finalize` | still a time leak if Grok skips finalize |
| `outcome`, `significance` | stencil must stay `alternate_remedy` / `ordinary` for 6.5 relegations |
| `family` | only `6.5-billing-relegation` and `contempt-6.5-dismissed` |
| `ok` | failures vs last batch |

## Review checklist

- [ ] `python tools/log_scj_review.py --summary` — elapsed by authoring; `demoted`; `citations>0` / `text>0`; short-uncited count.
- [ ] **Stencil false positives:** every `authoring=stencil` — extract vs JSON. Fail if the court refused 6.5, listed, withdrew, **granted** (411-class), or decided the bill. SCJ-353-class (`current bill` / `impugned bill`) should still be stencil TPs.
- [ ] **GRANT veto over-veto:** true 6.5 clones that now miss because the order says “forthwith” in a reconnection/stay sentence. If several, narrow the regex; do not drop `we intervene`.
- [ ] **Demotes:** for every `demoted=1`, skim extract vs JSON. Fail if the loop retried stencil on the same id.
- [ ] **Short-uncited that should have been full:** 3-page orders with **text citations** must **not** be `gate=short-uncited`. If `citation_count=0` on a 3-pager that names a reported case, tighten the counter, do not widen short-uncited.
- [ ] **6.5 clones still on short:** 1–2 page “apply under 6.5” with a bill/demand that did *not* stencil. Watch SCJ-408-class (`electricity amount due` / installments / restore). If several, a small BILL cue is on the table; do not widen into refusals or 7.10.
- [ ] **Coerce rate:** should stay low.
- [ ] **SCJ-328-class:** 4-page uncited doctrinal quash stays `gate=full` if words > 800. A 3–4 page quash with words ≤ 800 is still `short-words` (watch SCJ-430-class; don’t raise the floor unless quality dips).

Do not turn on listing-only from this review unless the stencil-FP check is clean *and* a separate dry-run on the pending queue agrees.
