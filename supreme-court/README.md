# Supreme Court Judgment Digests

Same folder structure and pipeline as `../high-court/`. Populate `input/` with searchable
Supreme Court judgment PDFs when you are ready to start this batch (typically once the High
Court set is underway or complete).
